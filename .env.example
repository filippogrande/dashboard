# Example environment variables for local/dev use
# Mount your services folder as the `data` folder or adjust `docker-compose.yml`

# Path inside container where services.json, images/ and compose files are located
SERVICE_ROOT=/srv/services

# Optional: Uptime Kuma integration (set to your Kuma URL to enable)
# Example: UPTIME_KUMA_URL=http://kuma.local:3001
UPTIME_KUMA_URL=
UPTIME_KUMA_API_KEY=

# Port the Flask app listens on (optional)
PORT=5000
